export const SEPARATOR = ':';

const OPEN = 'open';
const CLOSE = 'close';

export const POSITION = {
    OPEN,
    CLOSE,
};

export const COMMANDS = {
    open: 'o',
    close: 'c',
    startOpening: '<',
    startClosing: '>',
    stop: '|',
};
